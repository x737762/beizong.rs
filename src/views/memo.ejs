<div class="memo">
  <% memoList.forEach(item => { %>
    <div class="memo-item">
      <p class="content"><%= item.content %></p>
      <p class="time"><%= item.updateTime %></p>
    </div>
  <% }) %>
</div>
